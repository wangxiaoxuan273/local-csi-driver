apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    part-of: e2e-test
  name: lvm-pvc-no-annotation
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: local
