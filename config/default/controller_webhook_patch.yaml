apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: node
  namespace: system
spec:
  template:
    spec:
      containers:
      - name: driver
        ports:
        - containerPort: 9443
          name: webhook-server
          protocol: TCP
        volumeMounts:
        - mountPath: /tmp/k8s-webhook-server/serving-certs
          name: cert
          readOnly: true
      volumes:
      - name: cert
        secret:
          secretName: webhook-server-cert
