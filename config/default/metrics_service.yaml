apiVersion: v1
kind: Service
metadata:
  labels:
    control-plane: local-csi-driver
    app.kubernetes.io/name: csi-local
    app.kubernetes.io/managed-by: kustomize
  name: cluster-manager-metrics-service
  namespace: system
spec:
  ports:
  - name: https
    port: 8443
    protocol: TCP
    targetPort: 8443
  selector:
    control-plane: local-csi-driver
